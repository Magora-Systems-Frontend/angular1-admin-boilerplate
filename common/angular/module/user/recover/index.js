import controller from './recover.controller';
import template from './recover.jade';
import './recover.styl';

const recoverComponent = {
  template,
  controller,
  bindings: {},
};

export default recoverComponent;
